# Copy to .env and fill with secure values
# Do not commit .env

# Mongo
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_DB=sudpontino

# JWT
JWT_SECRET=change_me_generate_64_chars
JWT_REFRESH_SECRET=change_me_generate_64_chars
JWT_ACCESS_TTL=60m
JWT_REFRESH_TTL=14d

# CORS/frontend
FRONTEND_ORIGIN=http://localhost:5173
APP_PUBLIC_URL=http://localhost:5173

# Data encryption key (32 bytes). You can use base64 with prefix base64:
# openssl rand -base64 32
DATA_ENC_KEY=base64:REPLACE_WITH_BASE64_32B

# Email (optional)
EMAIL_REPLY_TO=
EMAIL_BCC=
